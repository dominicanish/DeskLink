# Core (cross-platform)
websockets>=12.0
zeroconf>=0.131

# Windows runtime (audio capture/playback + SMTC now-playing/transport)
PyAudioWPatch>=0.2.12 ; sys_platform == "win32"
winsdk>=1.0.0b10 ; sys_platform == "win32"
numpy>=1.24 ; sys_platform == "win32"

# Optional low-latency Opus codec (falls back to PCM if absent)
opuslib>=3.0

# Optional system-tray app (desklink-tray)
pystray>=0.19
Pillow>=10.0
